<%= form_with(model: stand, local: true) do |form| %>
  <% if stand.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stand.errors.count, "error") %> prohibited this stand from being saved:</h2>

      <ul>
      <% stand.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :area %>
    <%= form.text_field :area, id: :stand_area %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :stand_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :stand_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
